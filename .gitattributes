# Normalise to LF in the repo and in the working tree. Without this, Windows
# checkouts get CRLF and `prettier --check` fails in the pre-push hook.
* text=auto eol=lf

# Binary
*.png binary
*.jpg binary
*.ttf binary
*.otf binary
